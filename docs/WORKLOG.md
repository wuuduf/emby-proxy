# 2026-09-08：把 AI 编程流程落到本项目

## 本轮边界

基于 `codex/multiline-lab` 的 `26a45f3` 工作树，仅本地代码/测试/文档。
不接入服务器，不修改 DNS，不推送仓库，不更换技术栈，不拆分发布脚本。
采纳提供材料中的“上下文地图、小任务、先复现、验证、复审”，不照搬框架和目录模板。

## 已落地

- `AGENTS.md` 是简短入口；架构、行为约束、验证方法各有明确位置，README 保留上手用途。
- 新增 `scripts/check.py`：逐文件语法、内嵌 Python、SHA256、自动发现测试、超时、失败日志和汇总。
- 配置 CI 使用同一入口，覆盖 Linux/macOS 逻辑回归；不包含实机部署权限或凭据。
- 修复 `menu_update`：保留子进程隔离，同时将更新成功标记传回父进程；同版本内容更新不再丢失自动重启信号。
- 新回归先在旧实现复现 `same-version menu update lost UPDATE_CHANGED`，修复后通过。
- 菜单回归另覆盖未变化、取消、失败及此前遗留成功状态，不允许错误重启。
- 控制器状态机新增 6 个离线用例：优先级/配额边界、过期/无限额、一次性注册码、心跳认证/用量不倒退、DNS 失败保留活动节点并重试、节点 ID 冲突。
- 修复控制器测试假阳性：旧版固定 sleep 且不检查后台进程，在端口 bind 被沙箱拒绝时仍可能通过。现在必须完成 `/status` 就绪与发码后的 reconcile，并回收真实 Python PID，避免固定端口碰撞和残留进程。
- 验证器自身新增 3 个负面用例，确保失败、超时、第二个脚本语法错误和错误 SHA256 不会被掩盖。

## 验证证据与限制

- 修改前已有 6 套测试均报通过，但复核发现控制器测试的上述假阳性，不能将旧结果当作真实服务验证。
- 新测试在受限沙箱下正确报告 `PermissionError: Operation not permitted`；允许仅回环监听后可运行真实服务测试。
- 全量结果见运行后 `.test-results/summary.json` 和各套日志；不能把缓存报告当作下一次改动的验证。
- 发布校验值已随 manager 变更更新，安装器发布文件未改。
- 新 GitHub 工作流未推送、未在 GitHub 执行；未进行 VPS、真实 DNS、证书及 Emby 播放验收。

## 下一轮建议（按风险拆成单独任务）

1. **控制器重初始化保护**：当前 init 会重写已有状态。验收：已有节点/注册码时拒绝覆盖，显式确认并备份后才允许重建。
2. **真实入口健康探测**：进程 active 不能当作路由/TLS/源站可达。验收：三种失败可分别识别，暂时故障有阈值和防抖，不能为探测关闭 TLS 校验。
3. **用量统计闭环**：确定统计的是媒体输出还是网卡总流量、账期和重启行为。验收：请求增长、日志轮转、重启、手动重置都有证据，禁止只写 used_bytes 模拟自动超额。
4. **控制面边界**：注册地址 HTTPS、/status 信息最小化、请求体限制、注册码 TTL。`/reconcile` 已移除公网入口，状态文件权限已收紧；其余项目仍需单独设计协议和兼容策略。
5. **部署验证层**：加入真实 Caddy/Nginx 候选配置加载与隔离 systemd 集成测试；之后才安排用户授权的 VPS 演练。

每轮先确定一个验收目标，再补失败回归；不要把这张清单一次性改完或承诺全部完成。

## 后续：菜单与低配置向导

按最新要求转到菜单 UX；不重写部署引擎、选路算法或公网控制面。
- 主菜单保留 1–12，压缩首页状态和分组；中文标签取消按字节对齐；退出/返回支持 EOF。
- 多线路改为 1 配置、2 启动、3 添加节点，状态/日志独立，顶部显示状态和下一步。
- 状态路径不再问；源站可省略协议；节点名称可默认；配额一次输入，地址成功使用后自动沿用。
- 参数错误在当前字段重填；q 取消。先验证主控响应的入口 ID，再填写节点信息，失败不发码。
- 节点状态改为可读用量/心跳，但明确提示进程健康与计数文件的当前局限。
- 主控 systemctl 错误不再忽略；重新启动应用自己的新参数，不重启 Web 服务。
- 新菜单 UX 11 个用例在旧实现先复现 10 个失败，改动后全部通过；控制器离线回归增加地址记忆和服务失败，共 8 个用例。
- 统一完整本地检查此前为 **10/10 套通过**，含菜单、旧 CLI、两种引擎、三种入口结构、备份/更新与回环控制器。
- 设计及后续交互约定见 `docs/MENU-DESIGN.md`。这不是 VPS 或播放器实测结果；没有部署或上传。

## 后续：控制器边界与健康检查

- 控制器状态写入临时文件后显式设为 0600；已有 `controller init` 必须使用 `--force`，且会保留带时间戳的旧状态备份。
- 主控不再提供未认证的 POST `/reconcile`；systemd 主循环直接调用本地 `reconcile()`。
- 边缘心跳除检查 Caddy/Nginx 进程外，还通过 `curl --resolve 域名:443:127.0.0.1` 检查本机 HTTPS 健康地址，不使用 `-k` 绕过证书校验。
- 更新流程改为逐个执行两个发布脚本的 `bash -n`；新增回归覆盖“后端语法错误但 SHA256 正确”的情况。
- 本轮完整本地回归为 **11/11 套通过**。仍未完成账期重置、控制器 TLS/管理员认证、真实 VPS/DNS/Emby 播放验收。

## 后续：配额计数闭环

- 边缘节点现在从托管 Caddy/Nginx JSON 日志增量累计响应字节；保存日志 inode/偏移，重复心跳不会重复计算，轮转/截断会重新定位。
- `/_emby_proxy_health` 的响应不计入用量，已有计数只增不减；目前统计的是所有代理响应字节，不区分媒体/API，也没有账期重置。
- 多线路边缘要先具备可验证的本机 HTTPS 证书和健康路径；否则节点会保持不健康，不会被主控强行切入。
- 新增日志增量、轮转和健康路径排除测试；完整本地回归已重新执行，结果为 **11/11 套通过**。

## 后续：控制面公开信息

- `/status` 已缩减为入口 ID 和域名，添加节点的身份核对仍可用，节点状态继续通过本机 CLI 查看。
- 注册码现在记录签发时间，15 分钟后自动删除并拒绝注册；兼容旧版字符串注册码读取。
